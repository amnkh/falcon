---
- name: "Install Dependencies"
  apt:
    state: latest
    update_cache: yes
    pkg:
    - python
    - ntp
    - vim
    - wget
    - curl
    - screen
    - htop
    - smartmontools
    - sudo
    - ufw
    - apt-transport-https

- name: "Add TCP Rules to firewall"
  ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  with_items:
    - 443
    - 80
    - 22

- name: "Enable Firewall"
  systemd:
    name: ufw
    state: started
    enabled: yes
    masked: no
    daemon_reload: yes

- name: "Enable NTP"
  systemd:
    name: ntp
    state: started
    enabled: yes
    masked: no
    daemon_reload: yes
